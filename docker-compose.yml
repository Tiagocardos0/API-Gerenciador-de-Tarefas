services:
  api:
    container_name: Gerenciador-de-Tarefas-api
    build:
      context: .
      dockerfile: Dockerfile
    image: gerenciador-de-tarefas-api
    ports:
      - "3333:3333"
    env_file:
      - .env
    working_dir: /usr/src/app
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    depends_on:
      - postgres

  postgres:
    image: bitnami/postgresql:latest
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: gerenciador_de_tarefas
    volumes:
      - database:/bitnami/postgresql

volumes:
  database: